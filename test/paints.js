var paints = [{ color: 'red'}, { color: 'blue'}, {color: 'yellow'}];



function pluck (paints, property) {
    console.log(paint[property]);
  paints.map(function(paint,property){
    console.log(paint[property]);
    return paint[property];   
  });
}
  
var colors = pluck[paints,'color'];

colors

